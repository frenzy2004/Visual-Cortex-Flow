import React from 'react'
import ReactDOM from 'react-dom/client'
import VisualCortexFlow from './VisualCortexFlow.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <VisualCortexFlow />
  </React.StrictMode>
)
